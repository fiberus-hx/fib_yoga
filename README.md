# fib_meow

Haxe/fiberus @:native binding for the Yoga layout libs

### Setup

```bash
haxelib git fib_yoga https://github.com/fiberus-hx/fib_yoga.git
```

### Folders
`src` - contains the haxe externs
`lib` - contains the native code + dependencies